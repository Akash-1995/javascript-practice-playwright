//its control statements,it decide flow of test execution

let age=18;
if (age>=18){
    console.log(`You are eligible for the  vote`)
}
else{
    console.log(`you are not eligible for the vote`)
};


let result=age>=18? ' You are eligible for the vote' : 'You are not';

let marks=80;

if (marks>=35 && marks>50){
console.log(`c grade`)
}
else if(marks>=50 && marks>=70)
{
    console.log('b grade')

};

let user = "admin";
let isActive = true;

if (user === "admin") {
  if (isActive) {
    console.log("Admin is active");
  } else {
    console.log("Admin is inactive");
  }
}


let user = "admin";
let isActive = true;

if (user === "admin") {
  if (isActive) {
    console.log("Admin is active");
  } else {
    console.log("Admin is inactive");
  }
}
